{
  "schemaVersion": 1,
  "taskId": "B14",
  "contract": {
    "tabID": "opaque string (BrowserTabID type) - stable within tab lifetime, NOT CDP target ID",
    "sessionOwnership": "Tab belongs to session; cross-session operations rejected",
    "activeTabSemantics": "ActivateTab explicit call; no implicit global current tab",
    "tabLifecycle": ["loading", "ready", "failed"],
    "navigation": "BrowserNavigator interface - URL validation but does NOT replace PermissionBroker"
  }
}

{
  "schemaVersion": 1,
  "taskId": "B14",
  "contract": {
    "actions": ["click", "input", "select", "hover", "scroll"],
    "request": "BrowserInteractionRequest with SessionID, TabID, Element ref, action, optional input",
    "result": "BrowserInteractionResult with Success, Stale flag, optional error hint",
    "elementExpiry": "Stale element error if DOM changed since last reference",
    "noGlobalCurrentTab": "All operations require explicit SessionID + TabID"
  }
}

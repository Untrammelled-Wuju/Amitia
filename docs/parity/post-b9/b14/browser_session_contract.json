{
  "schemaVersion": 1,
  "taskId": "B14",
  "contract": {
    "sessionID": "opaque string (BrowserSessionID type) - stable within session lifetime, NOT OS PID or CDP websocket URL",
    "ownership": "Single provider per session",
    "lifecycleSemantics": ["created", "ready", "closing", "closed", "failed"],
    "concurrency": "Multiple sessions must be supported; global currentSession IS NOT allowed",
    "stateAuthority": "Browser Domain owns canonical state; Kernel Execution status stays separate",
    "cleanupSemantics": "CloseSession triggers tab cleanup; provider-specific engine teardown in B79-B83"
  }
}

{
  "schemaVersion": 1,
  "taskId": "B22",
  "generatedAt": "2026-08-08T17:00:00+08:00",
  "description": "Platform Invocation合同矩阵",

  "matrix": [
    {
      "contract": "Canonical request type",
      "android": "ToolInvocationContext (Canonical)",
      "ios": "ToolInvocationContext (Canonical)",
      "desktop": "ToolInvocationContext (Canonical)",
      "uniform": true
    },
    {
      "contract": "Invocation identity",
      "android": "InvocationID → RequestID (bridge DTO)",
      "ios": "InvocationID → RequestID (bridge DTO)",
      "desktop": "InvocationID → RequestID (bridge DTO)",
      "uniform": true
    },
    {
      "contract": "Context",
      "android": "ctx context.Context (Canonical)",
      "ios": "ctx context.Context (Canonical)",
      "desktop": "ctx context.Context (Canonical)",
      "uniform": true
    },
    {
      "contract": "Deadline",
      "android": "select ctx.Done()",
      "ios": "select ctx.Done()",
      "desktop": "select ctx.Done()",
      "uniform": true
    },
    {
      "contract": "Cancellation",
      "android": "select ctx.Done() → Cancelled",
      "ios": "select ctx.Done() → Cancelled",
      "desktop": "select ctx.Done() → Cancelled",
      "uniform": true
    },
    {
      "contract": "Trace",
      "android": "InvocationID propagating",
      "ios": "InvocationID propagating",
      "desktop": "InvocationID propagating",
      "uniform": true
    },
    {
      "contract": "Provider routing",
      "android": "binding.HandlerName → Android Provider",
      "ios": "binding.HandlerName → iOS Provider",
      "desktop": "binding.HandlerName → Desktop Provider",
      "uniform": true
    },
    {
      "contract": "Transport DTO",
      "android": "AndroidBridgeRequest (transportOnly=true)",
      "ios": "IOSBridgeRequest (transportOnly=true)",
      "desktop": "DesktopBridgeRequest (transportOnly=true)",
      "uniform": true,
      "note": "Bridge DTO仅为传输层，不拥有Runtime Authority"
    }
  ],

  "allSemanticsUniform": true,
  "bridgeDtoIsTransportOnly": true,
  "bridgeDtoHasNoRuntimeAuthority": true
}

{
  "schemaVersion": 1,
  "taskId": "B12-B55-B78-INPUT",
  "description": "B55-B78 Android Runtime/Provider实现输入",
  "generatedAt": "2026-08-08T01:50:00+08:00",

  "runtimeBinding": {
    "androidNative": "RuntimeType enum value for Android native provider - to be added by B55-B78",
    "androidEmbedded": "A线已有Android嵌入运行时，B12不重复"
  },

  "adapterRequirements": {
    "androidNativeAdapter": {
      "mustRegister": "RuntimeAdapterRegistry.Register(RuntimeTypeAndroid_Native, androidAdapter)",
      "mustImplement": ["Supports", "Execute", "Health"],
      "mustHonorPermission": "Before provider execution, call PermissionBroker.Evaluate() with SubjectForExtension(extID)",
      "mustReturn": "UnifiedToolResult from any Android domain DTO (ScreenshotResult, MotionEvent, etc.)"
    }
  },

  "providerRequirements": {
    "androidAccessibilityProvider": {
      "mustUse": "Android Native Bridge (Flutter/MethodChannel) or existing IPC",
      "mustNotCreate": "Second AndroidNativeBridge2 - must reuse A线已有Bridge",
      "mustPropagate": "ctx cancellation through IPC to Android native"
    },
    "androidNotificationProvider": {
      "mustUse": "Android NotificationManager via Bridge",
      "mustHandle": "user_action_required if notification permission not granted"
    }
  },

  "permissionIntegration": {
    "kernelPermissions": ["service.android.accessibility", "service.android.notification"],
    "platformAuthorization": "Android OS-level permission (handled by Provider, NOT Kernel)",
    "mustNotBypass": "All Android Provider executions must pass through PermissionBroker"
  },

  "executionChain": {
    "correctPath": "Tool -> ToolRegistry -> ExecutionPipeline -> PermissionBroker -> RuntimeAdapter(Android) -> AndroidNativeProvider",
    "forbiddenPath": "Tool -> AndroidNativeProvider (bypassing Kernel)"
  },

  "resourceHandling": {
    "scheme": "amitia://temp/... or amitia://workspace/...",
    "forbidden": "raw /data/... paths as portable resource"
  }
}

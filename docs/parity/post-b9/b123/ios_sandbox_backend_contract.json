{
  "schemaVersion": 1,
  "taskId": "B123",
  "generatedAt": "2026-08-08T17:00:00+08:00",
  "description": "B123 Sandbox Backend合约 - B124需要实现的最小Backend边界",

  "backendRole": "SANDBOX_BACKEND",
  "backendIdentity": {
    "productionName": "iSHSandboxBackend",
    "interimStatus": "UNAVAILABLE - 等待B124实现"
  },

  "backendInterface": {
    "name": "SandboxBackend",
    "proposedLocation": "backend/internal/extension/kernel/sandbox/backend.go (B124创建)",
    "requiredMethods": [
      {
        "method": "Availability",
        "signature": "Availability(ctx context.Context) BackendAvailability",
        "description": "返回后端可用性状态"
      },
      {
        "method": "Start",
        "signature": "Start(ctx context.Context, config SandboxConfig) error",
        "description": "启动Sandbox运行时"
      },
      {
        "method": "Stop",
        "signature": "Stop(ctx context.Context) error",
        "description": "停止Sandbox运行时"
      },
      {
        "method": "Execute",
        "signature": "Execute(ctx context.Context, cmd SandboxCommand) (SandboxResult, error)",
        "description": "在Sandbox内执行命令"
      },
      {
        "method": "Health",
        "signature": "Health(ctx context.Context) HealthStatus",
        "description": "健康检查"
      }
    ]
  },

  "availabilityStates": [
    "BACKEND_UNAVAILABLE - 后端未实现/未安装",
    "BACKEND_AVAILABLE - 后端就绪",
    "BACKEND_STARTING - 后端启动中",
    "BACKEND_RUNNING - 后端运行中",
    "BACKEND_ERROR - 后端错误"
  ],

  "boundaryConstraints": {
    "containsImplementationDetail": false,
    "exposesISHInternals": false,
    "exposesRootfsInternals": false,
    "returnsPortableErrors": true,
    "respectsContextCancellation": true
  },

  "errorMapping": {
    "backend_unavailable": "ErrorCodeNotAvailable",
    "backend_timeout": "ErrorCodeTimeout",
    "backend_execution_failed": "ErrorCodeExecutionFailed",
    "sandbox_not_ready": "ErrorCodeNotAvailable",
    "rootfs_corrupted": "ErrorCodeNotAvailable"
  },

  "forbiddenImplementations": [
    "FakeISHSandboxBackend (生产注册)",
    "NoopISHSandboxBackend (生产注册)",
    "PlaceholderISHSandboxBackend (生产注册)"
  ],

  "testOnlyImplementations": [
    "fakeSandboxBackend (*_test.go)",
    "mockSandboxBackend (测试代码)"
  ],

  "internalPathBoundary": {
    "sandboxPhysicalPath": "DTO only - 不作为Portable Resource",
    "resourceReference": "必须使用amitia:// scheme",
    "workspaceMapping": "通过SandboxConfig声明"
  }
}

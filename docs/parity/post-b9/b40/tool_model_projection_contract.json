{
  "schemaVersion": 1,
  "taskId": "B40",
  "canonicalSchemaSource": "ToolDefinition.InputSchema json.RawMessage",
  "projections": {
    "openai": "ToolFacade.buildKernelModelTools -> agent/tool.Parameters (Name + Parameters from schemaToParameters(def.InputSchema))",
    "anthropic": "same generic projection consumed by llm_client caller",
    "gemini": "same generic projection consumed by llm_client caller"
  },
  "canonicalAuthorityNeverChanges": true,
  "note": "Model projection is a thin adapter over the canonical InputSchema. It is not a second source of truth."
}

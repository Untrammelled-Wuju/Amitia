{
  "schemaVersion": 1,
  "taskId": "B40",
  "canonicalRepresentation": "json.RawMessage stored on ToolDefinition.InputSchema",
  "supported": {
    "string": true,
    "integer": true,
    "number": true,
    "boolean": true,
    "object": true,
    "array": true,
    "enum": true,
    "required": true,
    "optional": true,
    "nullable": "preserved as authored in raw JSON"
  },
  "unknownFields": "NOT_REQUIRED: passthrough; not structurally validated",
  "validationOwner": "execution.InputValidator (size-only currently)",
  "note": "Full JSON Schema structural validation is out of scope (B40 forbids adding a JSON Schema library and building a full engine)."
}

{
  "schemaVersion": 1,
  "taskId": "B15",
  "domain": "AUDIO_OUTPUT",
  "authority": "backend/internal/tts/",
  "interfaces": {
    "TtsService": "CRUD + Activate + Synthesize(variants) + Test + ListProviders",
    "Engine": "Multi-protocol synthesis (volcengine, openai, azure, edge, elevenlabs, minimax, aliyun, cosyvoice)",
    "VoiceClone": "Custom speaker creation and clone"
  },
  "config": {
    "table": "tts_configs",
    "fields": "ApiType, ApiKey, BaseURL, ResourceId, VoiceType, Emotion, Speed, Pitch, Volume, RealtimeAppId/Token/Key"
  },
  "providers": ["volcengine", "openai", "azure", "edge", "elevenlabs", "minimax", "aliyun", "cosyvoice"],
  "inputContract": {
    "type": "Text string + Voice configuration"
  },
  "outputContract": {
    "type": "SynthesizeResponse { AudioURL, Duration }",
    "note": "AudioURL is local path like /audio/<hash.mp3>; could be adapted to ResourceURI"
  },
  "stateProjection": {
    "authority": "Synthesis is synchronous; status tracked by caller"
  },
  "errorProjection": {
    "authority": "TTS Provider errors via fmt.Errorf wrapping"
  },
  "mapping": "ALREADY_SUPPORTED",
  "futureAdapterNote": "AudioURL could be adapted to amitia:// ResourceURI if needed"
}

{
  "schemaVersion": 1,
  "taskId": "B15",
  "domain": "IMAGE_ANALYSIS",
  "authority": "backend/internal/vision/",
  "interfaces": {
    "VisionService": "CRUD + TestConnection + ListProviders"
  },
  "config": {
    "table": "vision_configs",
    "fields": "ApiType, ApiKey, ModelName, BaseUrl, IsActive"
  },
  "providers": ["volcengine", "gemini", "openai-compatible"],
  "inputContract": {
    "type": "Image URL (base64 data URI or remote URL)",
    "note": "Vision uses Chat Completions API with image content; not a separate Vision API"
  },
  "outputContract": {
    "type": "Text description or structured analysis via Chat response",
    "note": "Vision output is structured result, NOT media resource"
  },
  "stateProjection": {
    "authority": "Chat Pipeline Status",
    "noMediaArtifact": true
  },
  "errorProjection": {
    "authority": "Chat Model errors + HTTP status codes",
    "note": "No dedicated Vision error domain; uses chat/model errors"
  },
  "mapping": "ALREADY_SUPPORTED"
}

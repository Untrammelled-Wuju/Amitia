{
  "schemaVersion": 1,
  "taskId": "B21",
  "generatedAt": "2026-08-08T15:00:00+08:00",
  "description": "Desktop Bridge合同",

  "required": true,
  "bridgeInterface": {
    "name": "DesktopProvider",
    "path": "backend/internal/extension/kernel/capability/adapter_desktop.go",
    "transport": "in-process function call (Go同一进程内调用)"
  },

  "implementation": {
    "path": "backend/internal/extension/kernel/desktop/desktop_provider.go",
    "pattern": "DesktopHostProvider包装DesktopHost，将InvokeAction结果转换为DesktopBridgeResponse"
  },

  "designDecision": {
    "note": "Desktop Adapter在同一Go进程内执行，不需要跨进程通信。DesktopHostProvider是DesktopHost的薄包装层，将现有InvokeAction结果适配为DesktopBridgeResponse格式。当未来需要跨Electron/Go边界时，可扩展为HTTP/WebSocket通信。"
  }
}

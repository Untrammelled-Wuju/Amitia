{
  "description": "Goal completion/progress facts供未来Memory步骤消费",
  "memoryCandidates": [
    {
      "type": "goal_completed",
      "fields": ["goalId", "goalType", "completionEvidence"],
      "trigger": "B31 Goal Status → achieved"
    },
    {
      "type": "goal_failed",
      "fields": ["goalId", "goalType", "failureReason", "failureCategory"],
      "trigger": "B31 Goal Status → abandoned"
    },
    {
      "type": "goal_progress",
      "fields": ["goalId", "progressDelta", "milestoneReached"],
      "trigger": "B31 Progress incremented significantly"
    }
  ],
  "note": "B31不写Memory。此结构仅说明哪些Goal facts未来可以作为Memory候选。"
}

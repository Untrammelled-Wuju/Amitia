# 待修复Bug列表

## Bug #1 - TestStagingManagerCleanup 在 Windows 上失败

**状态**: 未修复（硬性问题）

**描述**: `TestStagingManagerCleanup` 测试在 Windows 上失败。`os.RemoveAll(area.Path)` 返回 nil 错误，但目录仍然存在（`os.Stat` 返回 nil 错误）。

**文件**: `backend/internal/extension/kernel/package_security/package_security_test.go:490`

**复现**: `go test -run TestStagingManagerCleanup ./internal/extension/kernel/package_security/ -v`

**原因分析**: 这是 Windows 文件系统的已知问题。`os.RemoveAll` 在 Windows 上可能因为文件句柄延迟释放或防病毒软件锁定文件而返回成功，但实际目录未被删除。该问题与 MVP 修改无关，是预先存在的测试问题。

**影响**: 仅影响测试，不影响实际功能。在生产环境中，staging 目录的清理失败不会阻塞安装流程。

**尝试过的修复**:
- 重试机制（5次重试，每次间隔20ms） - 无效
- 二次删除确认 - 无效

## Bug #7 - TestOpenAPICoversExtensionBusinessRoutes 失败

**状态**: 未修复（预存在问题）

**描述**: `TestOpenAPICoversExtensionBusinessRoutes` 测试失败，OpenAPI 规范缺少大量扩展业务路由路径的文档覆盖。

**文件**: `backend/internal/extension/openapi_routes_test.go:47`

**复现**: `go test -run TestOpenAPICoversExtensionBusinessRoutes ./internal/extension/ -v`

**缺失路径包括**:
- /extensions/schedules/* （调度相关）
- /extensions/services/* （可信服务相关）
- /extensions/desktop/* （桌面贡献相关）
- /extensions/dev-mode/* （开发模式相关）
- /extensions/events/* （事件相关）
- /extensions/canary/* （金丝雀相关）

**原因分析**: OpenAPI 规范文档未跟随扩展子 API（scheduleAPI、desktopAPIAdapter、devModeAPI、eventAPI、canaryAPI 等）的路由注册进行同步更新。这是预先存在的文档覆盖问题，与扩展系统功能修补无关。

**影响**: 仅影响 OpenAPI 文档完整性测试，不影响扩展系统实际功能。

**修复方向**: 需要将所有扩展子 API 的路由路径补充到 OpenAPI 规范文档中。

## Bug #8 - extension 根包测试数据库迁移不完整

**状态**: 未修复（预存在问题）

**描述**: `go test ./internal/extension/...` 中 `internal/extension` 根包的 26 个旧版 Package/Lifecycle 测试失败，测试数据库缺少 `extension_versions`、`extension_schedules`、`extension_event_deliveries` 等表。

**文件**: `backend/internal/extension/*_test.go`

**复现**: `go test ./internal/extension`

**原因分析**: 根包测试夹具只执行了部分旧迁移，未同步当前扩展数据库完整迁移集合。`kernel` 及其全部子包测试均通过，该问题与 DesktopHost 和更新索引修改无关。

**影响**: 影响扩展根包旧版生命周期测试，可能掩盖兼容层回归；不影响本次专项包测试。

**修复方向**: 统一根包测试数据库初始化入口，使其复用生产迁移器并等待后台调度器退出。

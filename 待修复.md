# 待修复

- `go test ./...`：`backend/internal/chat/service_history_test.go` 仍使用 `character.RoleRuntimeProfile.SystemPrompt`，当前结构体已无该字段，导致测试编译失败。
- `go test ./...`：`backend/internal/extension` 的部分测试数据库未创建 `extension_versions` 表，安装测试失败。
- `go test ./...`：`backend/internal/interaction` 的 `TestOrchestratorCompletedEventAppendFailureRollsBackSQLiteComplete` 未得到预期的 outbox 追加失败。
- `go test ./...`：`backend/internal/system` 的测试数据库缺少 `characters.character_base` 字段，随后 `WebChatCreateConv` 测试触发空指针。

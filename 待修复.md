# 待修复

## 1. character.Character 模型缺 deleted_at 字段，与 baseline.sql 不一致

- 现象：`internal/character/model.go` 的 `Character` 结构体没有 `DeletedAt` 字段，但 `internal/migration/baseline.sql` 的 `characters` 表里有 `deleted_at DATETIME`（真实库同样存在该列），且 `internal/chat/prompt_builder.go:369` 用原生 SQL 查询 `... AND deleted_at IS NULL`。
- 影响：依赖 `AutoMigrate(&character.Character{})` 建表的测试夹具产出的 `characters` 表没有该列，导致 `internal/chat/chat_functional_test.go` 的 5 个功能用例全部报 `no such column: deleted_at`（`TestChatFunctional_NormalMultiLineReply`、`TestChatFunctional_SingleLineReply`、`TestChatFunctional_LongMultiLineSplit`、`TestChatFunctional_TenRoundConsistency`、`TestChatFunctional_IdempotentRequest`）。
- 状态：未修复。已确认与实时语音改造无关（属既有问题）。曾尝试在测试夹具里补 `ALTER TABLE characters ADD COLUMN deleted_at DATETIME`，但补列后仍继续报 `record not found` → `角色不存在`，说明还有第二层问题（见下条），故一并回退，避免留下半成品改动。

## 2. character.Repository 接口漏声明 GetRuntimeProfileForUser，类型断言永远失效

- 现象：`internal/character/repository.go` 的 `Repository` 接口没有声明 `GetRuntimeProfileForUser`，但 `*repository` 实现了该方法；`internal/chat/prompt_builder.go:361-366` 通过类型断言 `s.charRepo.(scopedRuntimeProfileRepository)` 想优先走这条路，实际永远断言失败，落到 `prompt_builder.go:369` 的原生 SQL 兜底分支。
- 影响：
  1. 兜底分支用 `conversationOwnerMatches(owner, userID)` 严格比对 owner，在单用户本地模式下会让 `user_id` 为 `local_user` / `default` 的角色被判为「角色不存在」。这正是上面功能用例补列后仍然失败的原因。
  2. 生产环境因为该列存在且数据可匹配，暂未暴露；但走了非预期路径。
- 建议修复方向（需评估后再动）：
  - 把 `GetRuntimeProfileForUser(id, userID string, includeLegacyDefault bool) (*RoleRuntimeProfile, error)` 声明进 `Repository` 接口，让断言成立、走 `chatLocalSingleUserMode()` 宽容匹配；
  - 或显式删除 `prompt_builder.go` 里的死断言分支，避免误导。
- 风险提示：改接口会让生产从「原生 SQL 严格 owner 比对」切到「scoped 仓库宽容匹配」，单用户本地模式下更宽松，需确认不会放进不该出现的角色。

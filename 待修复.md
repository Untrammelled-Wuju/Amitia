# 待修复问题

## TestOpenAPICoversExtensionBusinessRoutes 测试失败（预先存在）

- 文件：`backend/internal/extension/openapi_routes_test.go`
- 问题：大量扩展系统路由（schedules、services、desktop、dev-mode、events、canary、tasks、rollbacks、hooks、kernel、migrations、updates、journal 等）未在 `schema/openapi.json` 中注册，导致测试失败
- 原因：项目发展过程中新增了大量路由但未同步更新 OpenAPI 文档
- 影响范围：仅影响 OpenAPI 文档完整性检查，不影响实际功能运行
- 修复方向：将所有注册路由补充到 `schema/openapi.json` 中

# 待修复问题

## kernel 包三个测试失败（预先存在，非 R4 Fail-Closed 修改引起）

文件：`backend/internal/extension/kernel/`

失败测试：
1. `TestPackageGenerationInstallPersistsEvidenceAndReadModel` - 生成步骤缺少 evidence（部分步骤 ResultJSON 和 SideEffectEvidence 为空）
2. `TestPackageGenerationRecoveryRestoresUninstallQuarantine` - 卸载生成对账失败：quarantine metadata unavailable for current path resolution: PACKAGE_OPERATION_NOT_FOUND
3. `TestPackagePipelinePreviewIsolationAndIdempotency` - 回滚阶段 upsert package version record 失败：PACKAGE_OPERATION_STORAGE_FAILURE: upsert package version update

根因分析：
- 这三个失败由之前会话中对 saga 代码的修改引起（添加 `FinalizePackageOperation`、资源快照隔离逻辑、`FinalizeOperationAndReleaseLeaseTx` 等）
- R4 Fail-Closed 修改（RepositoryError 分类、Final Gate 三个 Fail-Open 分支修复、isRollbackSnapshotExempt）是增量修改，不影响这些代码路径
- 需要单独排查 saga 重构引入的回归问题

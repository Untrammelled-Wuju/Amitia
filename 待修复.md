# 待修复问题

## wasm_runtime ValidateDefinition 数据竞争

- 现象：`go test -race` 检测到 `TestConcurrentInvoke` 在 `wasm_runtime/definition.go:273-274` 处存在数据竞争。
- 根因：`ValidateDefinition` 函数在验证过程中对传入的 `*WASMRuntimeDefinition` 进行字段变异（normalize），涉及 7 处写操作（ModuleSHA256、MemoryLimitBytes、FuelLimit、EntryExport、MaxOutputBytes、MaxHostCalls、CallTimeout）。当多个 goroutine 并发调用同一 `def` 指针时，并发读-改-写触发 race condition。
- 影响范围：`wasm_runtime/definition.go:273-321`，涉及 `ValidateDefinition` 函数整体设计（验证+规范化混合）。
- 修复方向：将规范化逻辑与验证逻辑分离，或对 `WASMRuntimeDefinition` 增加 `sync.Mutex` 保护并发变异。
- 状态：预先存在问题，与本次扩展内核 E2E/泄漏验收工作无关，待后续修复。

# 待修复问题

## repair_baseline 测试超时失败（环境问题）

- 文件：`backend/internal/extension/kernel/repair_baseline/baseline_acceptance_test.go`
- 问题：`final_acceptance/suite.go` 中的 `verifyBuildCompiles` 函数使用 `exec.Command("go", "build", "./...")` 调用 go 命令，但系统 PATH 中没有 go 命令（go 安装在 `C:\Code\Go\bin\go.exe`），导致命令执行超时
- 原因：Go 编译器未添加到系统 PATH 环境变量
- 影响范围：仅影响 final_acceptance 测试套件中的编译验证步骤，不影响实际功能运行
- 修复方向：将 `C:\Code\Go\bin` 添加到系统 PATH，或修改 `verifyBuildCompiles` 函数支持自定义 go 路径

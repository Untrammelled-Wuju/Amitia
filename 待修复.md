# 待修复问题

## 1. Handler 测试 Uninstall 路由未注册（预先存在）
- 位置: `backend/internal/desktoppet/installation/handler_test.go`
- 测试: `TestHandler_Uninstall_NotFound`、`TestHandler_Uninstall_Failed_InternalError`、`TestHandler_Uninstall_Invalid_BusinessError`、`TestHandler_Uninstall_Success`
- 现象: HTTP 状态 = 404, 期望 200, body=404 page not found
- 原因: `newHandlerTestRouter` 仅调用 `RegisterRoutes`，但 DELETE `/installations/:installationId` 路由在 `RegisterReleaseRoutes` 中注册。`Handler.Uninstall` 方法存在但未在任何路由中注册（死代码），生产环境使用 `ReleaseHandler.UninstallInstallation`
- 影响: 不影响生产功能，仅影响测试退出码
- 修复方向: 将 DELETE 路由从 `RegisterReleaseRoutes` 移至 `RegisterRoutes`，或更新测试使用 `RegisterReleaseRoutes` 和对应的 stub

# 待修复问题

## 1. Extension 测试后台 goroutine 导致 FAIL（预先存在）
- 位置: `backend/internal/extension/`
- 现象: 所有单独测试都 PASS，但测试包总体返回 FAIL
- 原因: 测试中启动的后台 goroutine（事件投递轮询、调度轮询）查询不存在的表（`extension_event_deliveries`、`extension_schedules`、`extension_versions`），导致 goroutine 中的错误未被捕获
- 影响: 不影响功能，仅影响测试退出码
- 修复方向: 测试 setup 中应创建所有必需表，或后台 goroutine 应在测试清理时正确停止

## 2. Server 测试 nil pointer dereference（预先存在）
- 位置: `backend/cmd/server/services_test.go:32` -> `services.go:452`
- 现象: `TestNewAppServicesBuildsCoreServicesOnce` panic: nil pointer dereference
- 原因: 测试调用 `NewAppServices(app.NewAppContext(db, nil), nil)` 传入 nil config，`services.go:452` 访问 `config.AppCfg.DesktopPetRuntime.Enabled` 时空指针
- 修复方向: 测试应提供有效的 config 对象，或 `NewAppServices` 应处理 nil config

## 3. Installation 测试 Windows 文件删除时序问题（预先存在）
- 位置: `backend/internal/desktoppet/installation/uninstaller_test.go`
- 测试: `TestPurgeGenerationData_DeletesGenTaskDir`（line 224）、`TestUninstall_AlreadyUninstalled_DirAlreadyGone`（line 141）
- 现象: `os.RemoveAll` 返回 nil（成功），但目录实际未被删除；`waitForDirDeleted` 等待 2 秒并尝试 `cmd /c rmdir /s /q` fallback 后目录仍存在，导致 `t.Fatalf`
- 原因: Windows 文件系统时序问题，可能由 antivirus 实时扫描、文件句柄延迟释放或文件系统缓存导致 `os.RemoveAll` 返回成功但目录删除尚未完成
- 影响: 仅影响测试退出码，不影响生产功能（`Uninstall`/`PurgeGenerationData` 逻辑正确，`os.RemoveAll` 在生产环境中最终会完成删除）
- 修复方向: 增加 `waitForDirDeleted` 等待时间至 5 秒，或在 `os.RemoveAll` 后增加重试验证机制

# 







# Amitia 26.2.0 全新版本即将到来！











# 更新日志

## 桌面端 26.2.0-beta 更新

### 启动与桌面环境

- 首次引导全面重做，支持本地运行和云端运行，并完成设备、访问密码和运行环境检查。
- 引导流程支持语言模型、语音模型、语音识别、视觉模型、向量模型和安全边界配置。
- 支持首次设备设置码、普通设备配对、Web 访问密码和云端连接状态检查。
- 桌面客户端支持本地模式和云端模式切换，并显示服务地址、核心状态、数据位置和访问方式。
- 完善桌面核心启动、停止、重启、超时、退出清理和异常恢复。
- 启动失败时显示端口占用、文件缺失、配置错误等明确原因和处理建议。
- 桌面标题栏支持前进、后退、缩放、全屏、最小化、最大化和关闭。
- 桌面菜单支持撤销、重做、剪切、复制、粘贴、删除和全选。
- 深度集成系统托盘、全局快捷键、原生菜单、剪贴板、文件选择器和目录选择器。
- 完善检查更新、更新内容查看、下载进度、稍后重启和立即安装。
- 更新失败时保留备用下载入口和明确错误提示。
- 新增亮色、暗色、主题色调和减少动画设置。
- 重构侧边栏、导航、页面标题、状态栏、错误恢复和 404 页面。
- 新增全局搜索，可查找角色、会话、记忆和应用功能。
- 新增概览页面，集中显示服务健康、运行状态、使用统计、导入记录和最近错误。
- 新增界面提供者体系，可按云端、用户、平台、设备和使用环境替换应用界面。
- 界面提供者支持替换应用框架、工作区、聊天侧栏、页面、通知和主题，并在异常时回退。

### 聊天与消息

- 重构聊天页面、会话列表、消息加载、流式回复、停止生成和重新生成。
- 支持会话切换、历史恢复、断线恢复和生成中断。
- 支持发送图片、视频、文件，并显示图片、视频、文档和语音播放控件。
- 支持任务产物上传、下载、内容预览和跨设备文件引用。
- 支持为消息选择 Agent Skill，并可通过斜杠菜单快速选择技能。
- 支持为当前对话设置工作目录和最近使用目录。
- Agent 可使用浏览器、深度搜索、虚拟显示、网页上传下载和跨设备工具。
- 支持多步骤任务规划、多代理协作、后台任务执行和长任务恢复。
- 支持查看用户画像、注入记忆、相关记忆和会话摘要。
- 新增日程提醒，支持重复规则、发送渠道、角色关联、触发历史和自动清理。
- 新增聊天记录查看，支持历史筛选、上下文预览和摘要。
- 新增聊天记录导入，支持预览、批量确认和从导入记录提取记忆候选。
- 新增语音通话、视频通话和实时多模态通话。
- 实时通话支持语音识别、语音合成、情绪识别、自动语音检测、摄像头和屏幕共享。
- 支持文字输入和按住说话两种输入模式。
- 扩展可在聊天顶部、状态区、输入区、消息旁、空状态和侧栏增加界面。
- 扩展支持自定义消息渲染、消息操作、消息标记、会话投影和动态会话节点。
- 新增微信连接管理，可扫码连接、重新连接、查看状态和统计消息。
- 新增 QQ 连接管理，可配置 Bot、切换沙箱模式、重新连接和统计消息。

### 角色与陪伴

- 新增角色卡导入导出，可复用和迁移角色配置。
- 支持角色提示词、身份设定、说话风格、关系风格和安全边界配置。
- 支持角色性格、性别画像、默认角色和测试聊天。
- 新增角色生活规则、场景规则和生活状态。
- 新增关系时间线，支持关系阶段、重逢识别和记忆引用设置。
- 新增心理状态页面，可查看情绪、信念、关系和冲突状态。
- 新增角色独立语音，可选择预设音色或配置声音克隆。
- 支持音色试听、语速、音调和音量调整。
- 新增主动消息、表情和生活助手插件，可独立安装、更新和配置。
- 支持从聊天或播放状态触发桌宠动作。

### 记忆与数据

- 新增候选记忆生成、审核、编辑、批量接受和忽略。
- 支持记忆冲突处理和记忆去重。
- 支持跨结构化记忆、情景记忆、画像和世界书的全局搜索。
- 支持召回分析、排序结果查看和嵌入索引重建。
- 记忆系统新增 L1-L5 记忆分层，并支持候选审核、冲突处理、图谱、隐私扫描、备份与迁移。
- 支持记忆强度、分层保留、淡化、归档和恢复。
- 新增用户画像管理，可查看和调整提取出的用户特征。
- 新增情景记忆，支持层级、强度和状态管理。
- 新增世界书，支持正则、精确和关键词匹配并支持 JSON 导入导出。
- 新增记忆时间线，可按来源、类型和记忆类别筛选并导出 CSV。
- 新增记忆图谱，可查看记忆、画像、情景和世界书之间的关系。
- 新增隐私扫描、敏感数据扫描历史、扫描结果和脱敏处理。
- 支持跨记忆、消息、会话和角色执行定向数据删除。
- 新增聊天数据清理、数据库健康检查和数据迁移。
- 支持配置导入导出、加密备份和备份恢复。
- 新增运行日志、模型错误记录、使用统计和 Prompt Trace。

### 创意工坊与桌宠

- 新增桌宠制作向导，支持参考图片、生图模型、提示词和负面提示词。
- 支持选择、排序和组合桌宠动作。
- 支持无需外部 API 的本地背景移除、画布适配、锚点检测和帧质量检查。
- 支持桌宠生成任务列表、实时进度、错误查看、取消和重试。
- 支持处理版本切换和动作结果预览。
- 新增处理结果审核，可查看动作、帧、质量问题并排除动作。
- 新增动作编辑器，支持时间轴、帧预览、时长、锚点、FPS 和循环方式。
- 动作编辑器支持优先级、冷却、可打断和返回动作配置。
- 支持桌宠资源打包、安装、重新安装和版本升级。
- 支持桌宠安装管理、资源校验、启用、停用和卸载。
- 支持调整桌宠大小、置顶、开机恢复、待机和声音。
- 支持设置默认待机动作和用户默认动作。
- 支持 `.petx` 桌宠插件管理。
- 桌宠支持拖拽、点击穿透、待机和活力状态。
- 桌宠会根据聊天、说话、播放和世界状态自动切换动作。
- 新增动画队列、动作抢占、播放恢复、帧缓存、预加载和资源解码。
- 动画系统支持透明度点击检测、帧时间线和播放状态恢复。
- 新增工作流列表，支持手动创建、AI 创建、AI 修改和运行分析。
- 支持工作流模板保存和复用。
- 新增可视化工作流编辑器，支持节点拖拽、连线、缩放、缩略图和属性面板。
- 工作流支持简单编辑和高级编辑模式。
- 工作流支持本地、云端和指定设备运行。
- 设备事件、语音唤醒、通知、电量、网络、应用和蓝牙触发器运行在本地或指定设备上，云端工作流通过设备执行链调用。
- 工作流支持正式执行、仅验证、模拟测试和副作用确认。
- 工作流支持运行历史、执行追踪、失败恢复和补偿动作。
- 工作流支持版本快照、回滚、嵌套工作流和跨设备执行。

### 扩展平台

- 扩展中心重新设计，支持安装、启用、停用、更新、回滚、诊断和权限管理。
- 支持 `.amitiax`、`.gamex` 和 `.petx` 扩展包。
- 安装前显示版本、权限、风险和兼容性预览。
- 支持扩展权限查看、授权和撤回，并在更新后保留用户选择。
- 支持扩展下载、更新、安装、回滚和更新历史。
- 支持扩展发布者信任、签名校验、密钥管理和完整性保护。
- 支持网络沙箱、能力权限和敏感资源隔离。
- 扩展可在聊天、桌面和设置中增加页面、按钮、状态和快捷操作。
- 扩展支持自定义导航入口、主题图标、消息渲染和会话投影。
- 扩展支持沙箱网页界面、客户端插件和动态界面会话。
- 新增插件 SDK、插件命令行工具、开发工作区、热重载和构建历史。
- 新增扩展更新中心、可信服务、WASM、Hook、任务和调度页面。
- 新增扩展事件、诊断控制台、调用追踪和诊断包导出。
- 新增扩展迁移、灰度发布和回滚管理。
- 新增桌面扩展贡献中心和快捷键冲突管理。
- 新增 MCP 服务管理，支持远端服务；本地程序模式仅在具备本地运行能力的桌面或自托管环境可用。
- MCP 支持 OAuth、工具、资源、提示词、目录授权和模型采样。
- MCP 支持交互确认、能力开关、异步任务、任务取消和调用日志。
- MCP 模型采样仅在用户批准后执行，不会向 MCP 服务提供模型密钥、角色记忆或完整会话历史。
- 新增 Agent Skills 管理，支持导入、兼容性报告和文件查看。
- Agent Skills 支持依赖计划，外部 MCP 连接和已安装本地程序必须分别确认。
- Agent Skills 自带脚本只允许查看，不会自动下载、安装或直接执行。
- Agent Skill 中的 `allowed-tools` 只是能力声明，不会直接授予权限。
- 新增游戏模式，可安装、更新、启用和停用游戏扩展。
- 游戏模式支持查看运行连接、控制模式和游戏插件状态。
- 游戏扩展可提供专属控制页面。

### 设备、云端与工作区

- 新增“我的设备”页面，可查看当前设备、云端设备、在线状态和同步状态。
- 支持生成一次性设备二维码、配对链接和首设备设置码。
- 支持移除设备、解除当前设备云端绑定和清除本机凭据。
- 支持通过 Web 登录使用桌面端功能。
- Web 设备使用独立设备身份和云端访问凭证。
- Web 浏览器固定作为云端设备运行，不提供切换为本地 Core 的选项。
- Web 访问支持安装为独立窗口的 Web 应用，并在新版本可用时提示刷新。
- Web 功能按运行环境能力显示，系统托盘、全局快捷键和本地程序启动等桌面专属能力不会出现在 Web 设备中。
- 支持云端设备执行、远程工作流和远程任务。
- 新增工作区，可浏览本地、SFTP、WebDAV 和远程文件。
- 支持工作区文件预览、目录切换和返回上级。
- 支持查看 Git 状态、暂存、还原、分支、远程仓库和提交历史。
- 支持管理工作区凭据、远程仓库和只读状态。
- 支持创建隔离工作区。
- 聊天可直接选择工作区目录，文件和 Agent 工具默认在该目录运行。

### 模型与设置

- 设置中心重新划分为运行、系统、通知、模型、安全、维护、日志、界面提供者和关于。
- 模型配置拆分为语言模型、语音模型、语音识别、视觉模型、向量模型和生图模型。
- 支持在线模型和本地模型。
- 模型来源与应用运行模式相互独立，本地模型表示模型服务地址，不等同于桌面本地核心模式。
- 支持连接测试、默认模型和场景分配。
- 支持显式模型列表和自定义模型。
- 语言模型支持 OpenAI 兼容服务和本地服务。
- 语音模型支持语音类型、语速、音调和音量。
- 语音识别支持 Provider、模型、Base URL 和音频测试任务。
- 视觉模型支持图片理解、独立视觉模型、复用语言模型和禁用模式。
- 向量模型用于长期记忆语义检索，支持连接测试。
- 生图模型支持图片生成、模型选择、连接测试和场景分配。
- API Key 仅保存在当前部署后端。
- 新增界面提供者设置，可分层配置不同设备和运行环境的界面。
- 新增通知设置，支持订阅、取消订阅和发送测试通知。
- 新增时间与地区设置，支持时区、语言、每周起始日、安静时段和地区感知。
- 支持节日、时段、纪念日和记忆时间共振设置。
- 新增安全设置，可限制情绪绑架、排他依赖、现实关系隔离和惩罚性表达。
- 支持禁用冒充真人、敏感主动提及和成人内容限制。
- 支持负面情绪强度、亲密表达强度和违规处理策略。
- 新增安全审计、访问认证、允许来源和限流设置。
- 新增高级系统设置，可管理运行观测、模型错误、语音会话和渠道服务。

### 维护与诊断

- 新增一键诊断，可检查系统组件和服务状态。
- 支持导出诊断包和查看导出记录。
- 支持重启微信和 QQ Bridge。
- 支持重新加载运行配置。
- 新增长期运行维护、临时文件清理和日志轮转。
- 新增运行时协作调试，可查看交互状态、预算、队列、投递、工具结果和一致性检查。
- 新增决策可视化，可查看运行快照和降级状态。

### 行为调整

- 主动消息、表情和生活助手改为扩展插件提供。
- MCP 和 Agent Skill 不再绑定单个角色，改为当前空间可用。
- 桌宠不再绑定单个角色，切换角色后可继续使用已安装桌宠。
- 游戏扩展仍在安装时展示权限和风险，运行期间由系统根据已授权状态直接判断，不再重复弹出审批。
- 旧的账号登录和首次后台管理员设置流程已移除，改用设备配对和新的 Web 访问流程。
- 旧 companion、emote 和 proactive 内置界面已移除，对应能力改由独立插件提供。

### 主要修复

- 修复聊天消息覆盖用户输入、顺序错误、重复显示和会话恢复失败。
- 修复聊天连接中断、历史消息加载异常和会话切换不同步。
- 修复扩展安装后无法启动、权限丢失、更新失败和页面显示 404。
- 修复扩展沙箱页面无法显示部分图片。
- 修复设备配对、远程执行、云端绑定和桌面端会话恢复失败。
- 修复桌宠恢复、角色切换、动画播放和生成结果尺寸不一致。
- 修复首次启动或版本升级时部分数据迁移失败导致服务无法启动。
- 修复桌面标题栏、侧边栏、主题、窗口缩放和部分页面布局。







### 更新重点

- 桌面端完成本地与云端运行、设备配对、Web 访问和界面提供者体系升级。
- 扩展平台成为统一入口，支持新扩展包、权限、更新回滚、开发工具和沙箱 UI。
- 新增完整桌宠系统，支持制作、动作编辑、安装管理、动画播放和实时行为。
- 新增可视化工作流，支持 AI 创建与修改、设备触发、版本回滚和跨设备执行。
- 聊天新增多模态通话、消息附件、技能选择、工作目录、提醒和扩展会话投影。
- 新增游戏模式，支持游戏扩展安装、运行连接、控制模式和专属游戏页面。
- 新增 MCP、Agent Skills 和跨设备 Agent 工具能力。
- 记忆系统新增 L1-L5 记忆分层，并支持候选审核、冲突处理、图谱、隐私扫描、备份与迁移。
- 模型配置覆盖语言、语音、语音识别、视觉、向量和生图，并支持在线与本地模型。
- 桌面端深度集成系统托盘、全局快捷键、更新、剪贴板和多窗口相关能力。
- 新增统一诊断、运行日志、Prompt Trace、决策可视化和安全审计工具。







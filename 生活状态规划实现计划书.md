# 冷落感知 + 情绪追问 + 自适应频率 — 实现计划书

> 创建日期：2026-06-05
> 项目：U-Ai companion + proactive 模块
> 目标：AI 感知用户冷落，情绪动态变化，主动消息频率自适应

---

## 一、现状

以下功能**均未实现**：

| 功能 | 现状 |
|---|---|
| 感知用户多久没回复 | 无。energy/mood 纯时间驱动，不查 messages 表 |
| 情绪因冷落而低落 | 无。mood 永远 neutral，除非手动写入 moods 表 |
| 追问"为什么不理我" | 无。没有 chase_up 类型任务 |
| 长时间不回复后降频 | 无。频率由 active_message_setting 静态控制 |

---

## 二、设计方案

### 2.1 冷落时长计算 (`idleDuration`)

数据源：`messages` 表中 `role = user` 的最新一条 `created_at`。

```
idleDuration = now - lastUserMessageAt
```

新增方法：`func (s *service) getIdleDuration() time.Duration`

### 2.2 情绪降级规则

在 `GetStateLife` 的 mood 计算中注入：

| idleDuration | mood | 表现 |
|---|---|---|
| < 6h | 正常（读 moods 表/默认 neutral） | 无变化 |
| 6h – 12h | `lonely` | "有点想你了"风格 |
| 12h – 24h | `ignored` | 追问生成 |
| 24h – 48h | `sad` | 失落 |
| > 48h | `depressed` | 低功耗模式 |

mood 优先级：`moods` 表最新记录 **低于 2 小时** 时优先于冷落计算，超过 2 小时则以冷落计算为准。

### 2.3 追问任务 (`chase_up`)

在 `ScheduleBasedGenerator` 中新增规则：

```
if idleDuration > 12h:
  chaseUpTime = now + random(5, 15) 分钟
  prompt = "你已经超过{idleDuration}没收到回复了。你有点失落。请生成一条自然的追问，1-2句，不要指责对方。"
  write active_message_task(type=chase_up)
```

追问冷却：24 小时内只发一次。

### 2.4 自适应频率降级

在 `ScheduleBasedGenerator` 计算 `maxTasks` 时，注入活跃度系数：

| idleDuration | maxTasks | 保留的任务 |
|---|---|---|
| < 2h | 5（正常） | 全部 |
| 2h – 6h | 3 | morning + noon + bedtime |
| 6h – 12h | 2 | morning + bedtime |
| 12h – 24h | 1 + chase_up | bedtime + 追问 |
| 24h – 48h | 1 | 仅 bedtime |
| > 48h | 0 | 静默（不发新任务） |

### 2.5 低功耗模式

`idleDuration > 48h` 时：
- `ScheduleBasedGenerator` 不生成任何新任务
- `RandomBurstTrigger` 强制跳过
- `scanActiveTasks` 正常处理已有任务，但新任务不再生成
- 用户重新发消息后自动恢复

### 2.6 恢复机制

用户发送新消息后（`ProcessMessage` 中新增检测）：
- 重置 `lastUserMessageAt`
- 如果之前处于 low mood → 生成一条 `reconnect` 任务（"你终于回来了"）
- 下次 `ScheduleBasedGenerator` 自动恢复正常频率

---

## 三、实现步骤

| 步骤 | 内容 | 文件 |
|---|---|---|
| 1 | `getIdleDuration()` — 查 messages 表最新 user 消息时间 | `service.go` |
| 2 | `GetStateLife` 注入冷落 mood 降级逻辑 | `service.go` |
| 3 | `ScheduleBasedGenerator` 新增 chase_up 规则 + 追问冷却 | `service.go` |
| 4 | `ScheduleBasedGenerator` maxTasks 注入活跃度系数 | `service.go` |
| 5 | `RandomBurstTrigger` 低功耗模式跳过 | `proactive_cron.go` |
| 6 | `ProcessMessage` 中新增恢复检测 + reconnect 任务 | `chat/service.go` |
| 7 | `GetDebugOverview` 暴露 idleDuration 字段 | `service.go` |
| 8 | 编译验证 + 重启 | — |

---

## 四、修订记录

| 日期 | 修订内容 |
|---|---|
| 2026-06-05 | 初版：冷落感知、情绪追问、自适应频率完整方案 |

# 项目记忆系统增强规划

---

## 一、短期工作记忆层（对话窗口管理）

### 当前现状

项目目前的对话上下文管理位于 backend/internal/chat/service.go：loadHistory() 全量加载无窗口限制，ProcessMessage() 直接拼接全部历史，messages 表 include_in_context 字段闲置。

### 细化需求1：滑动窗口上下文管理器

- config.go：ChatConfig 新增 ContextWindowMaxRounds 字段，默认20
- config.yml：新增 contextWindowMaxRounds: 20
- service.go：loadHistory() 增加 WHERE include_in_context=1 + LIMIT 过滤，新增 getWindowMaxRounds()、trimContextWindow()
- sql.sql：新增 idx_messages_conv_ctx 索引

### 细化需求2：工作记忆缓存结构

- 新建 working_memory.go：WorkingMemory 结构体 + WorkingMemoryCache（mu+store+expireAfter）
- service.go：结构体加 wmCache 字段，ProcessMessage 注入+更新工作记忆
- model.go：新增 WorkingMemoryState 结构体

### 细化需求3：自动标记超出窗口的消息

- 由 trimContextWindow() 覆盖实现

### 改动影响范围汇总

| config.go | 修改 | ChatConfig 加字段 |
| config.yml | 修改 | 加配置项 |
| service.go | 修改 | 7处改动 |
| sql.sql | 修改 | 新增索引 |
| working_memory.go | 新建 | 缓存实现 |
| model.go | 修改 | 新增结构体 |

**前端无需改动。**

---

## 二、用户画像记忆层（结构化实体画像）

### 当前现状

无独立用户画像存储，用户事实混在 memories 表中，无置信度、无来源追溯。

### 细化需求1：新建 user_profiles 表与后端模块

**新建 backend/internal/profile/ 模块（model/repository/service/handler/router 5文件）**

- UserProfile 表：user_id、category(personal_info/preference/habit/fear/relationship/health/plan)、attribute_name、attribute_value、confidence(0-100)、source_conv_id、verified_at
- 唯一索引 (user_id, category, attribute_name)，UpsertConfidence 去重+自动提置信度
- ExtractFromConversation：LLM 提取画像事实
- ToSystemPrompt：格式化为【用户画像】标签注入
- sql.sql 新增 user_profiles 表 + 3索引

### 细化需求2：对话流程画像提取

- chat/service.go：service 加 profileSvc，ProcessMessage 注入+触发提取
- cmd/server/main.go：初始化 profSvc 传入 chatSvc
- agent/tool/memory.go：新增 save_profile 工具

### 细化需求3：前端画像视图

- **front/src/views/profile/ProfileView.vue（新建）**：类别筛选+卡片网格+置信度进度条+来源追溯+行内编辑
- **front/src/composables/useProfile.ts（新建）**
- **front/src/router/index.ts**：新增 /profiles 路由
- **front/src/views/web-chat/WebChatView.vue**：侧边栏新增用户画像摘要面板

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| backend/internal/profile/（5文件） | 新建 | 完整画像模块 |
| backend/data/sql.sql | 修改 | 新表+索引 |
| backend/cmd/server/router.go | 修改 | 注册路由 |
| backend/internal/chat/service.go | 修改 | 注入+触发 |
| backend/cmd/server/main.go | 修改 | 初始化 |
| backend/internal/agent/tool/memory.go | 修改 | save_profile |
| front/src/views/profile/ProfileView.vue | 新建 | 画像管理页 |
| front/src/composables/useProfile.ts | 新建 | API |
| front/src/router/index.ts | 修改 | /profiles |
| front/src/views/web-chat/WebChatView.vue | 修改 | 侧边栏面板 |

---

## 三、长期情景记忆层（片段化情景存储）

### 当前现状

无情景记忆存储，对话中感悟/笑话/情感峰值等片段未保留。memory_events 仅记录 CRUD 事件，非对话时间线。

### 细化需求1：新建 episodic_memories 表与后端模块

**新建 backend/internal/episodic/ 模块（5文件）**

- EpisodicMemory 表：scene_type(insight/joke/milestone/emotional_peak/confession)、title、content、context_before/after（前后各2轮快照）、trigger_keywords、sentiment_score(-10~+10)、message_id_start/end
- ExtractFromConversation：LLM 检测情景，提取上下文快照
- ToSystemPrompt：格式化【情景记忆-最近】注入
- GetDetail：返回情景+原始消息回溯
- sql.sql 新增表 + 3索引

### 细化需求2：聊天流程情景检测

- chat/service.go：service 加 episodicSvc，ProcessMessage 注入+触发 detectEpisodicMoment
- cmd/server/main.go：初始化 epiSvc 传入 chatSvc
- agent/tool/memory.go：新增 save_episodic_memory 工具

### 细化需求3：前端情景回忆视图

- **front/src/views/episodic/EpisodicView.vue（新建）**：场景类型时间轴+上下文气泡回溯+情感分值
- **front/src/composables/useEpisodic.ts（新建）**
- **front/src/router/index.ts**：新增 /episodic 路由

### 改动影响范围汇总

| backend/internal/episodic/（5文件） | 新建 | 完整模块 |
| backend/data/sql.sql | 修改 | 新表+索引 |
| backend/cmd/server/router.go | 修改 | 注册路由 |
| backend/internal/chat/service.go | 修改 | 注入+触发 |
| backend/cmd/server/main.go | 修改 | 初始化 |
| backend/internal/agent/tool/memory.go | 修改 | save_episodic_memory |
| front/src/views/episodic/EpisodicView.vue | 新建 | 情景回忆页 |
| front/src/composables/useEpisodic.ts | 新建 | API |
| front/src/router/index.ts | 修改 | /episodic |

---

## 四、结构化事实记忆升级（从 Key-Value 到多维事实）

### 当前现状

Memory 模型仅11个字段，CheckConflict 用 LIKE 字符串匹配，注入为 - value 平铺列表。

### 细化需求1：memories 表结构升级

Memory 结构体新增8字段：Confidence(0-100)、ExpiresAt、EntityID/Type、SourceMsgID/ConvID、VerifiedStatus(unverified/auto_confirmed/user_verified/contradicted)、LastVerifiedAt
sql.sql ALTER TABLE 追加8列 + 4索引

### 细化需求2：冲突检测升级

CheckConflict 三层检测：精确key匹配→语义向量相似(>0.85)→LLM矛盾判断
ResolveConflict 新增 AutoResolveConflict 自动消解策略（置信度差≥40自动替换）
Create 方法增加冲突前置检查

### 细化需求3：上下文注入升级

从 - value 平铺改为优先级分类：高置信度/高重要性单独【关键记忆】标签+typeLabel+置信度数值
新增 GetRankedMemories 三路合并排序（向量0.4+关键词0.3+重要性0.3）
Agent save_memory 新增 confidence/expiresAt/entityId 参数

### 细化需求4：前端记忆管理增强

- MemoryManagerView.vue：新增置信度进度条列、过期状态列（已过期/已确认/有矛盾/未核实）、批量操作(确认/标为重要/删除)
- MemoryPanel.vue：卡片增加置信度标签(>80绿/50-80黄/<50红)、过期提示、核实状态图标

### 改动影响范围汇总

| backend/internal/memory/model.go | 修改 | 新增8字段 |
| backend/data/sql.sql | 修改 | ALTER+4索引 |
| backend/internal/memory/service.go | 修改 | 三层冲突+自动消解+GetRankedMemories |
| backend/internal/memory/repository.go | 修改 | 新增方法 |
| backend/internal/chat/service.go | 修改 | 注入升级 |
| backend/internal/agent/tool/memory.go | 修改 | 新参数 |
| front/src/views/memory-manager/MemoryManagerView.vue | 修改 | 新列+批量 |
| front/src/components/MemoryPanel.vue | 修改 | 新标签 |

---

## 五、向量检索增强（从单一检索到多层检索）

### 当前现状

单一 collection "memory_embeddings"，所有记忆向量混存。

### 细化需求1：多 Collection 架构

config.go/config.yml：QdrantConfig 从单 collectionName 改为 map[string]CollectionConfig（memory_embeddings/working_memory/user_profiles/episodic_memories）
client.go：所有函数增加 collectionName 参数，新增 MultiSearch 跨集合搜索，EnsureCollections 替代 EnsureCollection

### 细化需求2：各模块同步向量到对应 Collection

SyncEmbedding 按 memoryType 路由到对应 collection
VectorSearch 跨4 collection 搜索合并
chat/service.go ProcessMessage 注入从单一【相关记忆】改为分层标注（当前摘要/用户画像/情景回忆/事实记忆）

### 细化需求3：混合检索模式

新增 HybridSearch：关键词+向量 双路→按 memory_id 去重→matchType标记→合并排序
新增 POST /memories/hybrid-search 路由

### 改动影响范围汇总

| backend/config/config.go | 修改 | 多collection配置 |
| backend/config/config.yml | 修改 | collections节点 |
| backend/pkg/database/qdrant/client.go | 修改 | 所有函数加collectionName参数 |
| backend/internal/memory/service.go | 修改 | SyncEmbedding路由+HybridSearch |
| backend/internal/chat/service.go | 修改 | 分层标注注入 |
| backend/cmd/server/main.go | 修改 | EnsureCollections |
| front/src/views/memory-manager/MemoryManagerView.vue | 修改 | 向量状态栏升级为多collection表格 |

---

## 六、世界书机制（正则/关键词触发记忆注入）

### 当前现状

无世界书功能，无规则匹配基础设施。

### 细化需求1：新建 world_book 表与后端模块

**新建 backend/internal/worldbook/ 模块（5文件）**

- WorldBookEntry 表：match_type(regex/exact/keyword)、match_pattern、match_scope(user_message/assistant_reply/full_context)、inject_content、priority、hit_count
- MatchAndCollect 匹配引擎：三模式匹配+正则预编译缓存+5分钟TTL内存缓存
- TestMatch 端点：在线测试匹配
- sql.sql 新增表 + 2索引

### 细化需求2：上下文组装前匹配

chat/service.go：service 加 worldBookSvc，ProcessMessage 中 systemPrompt 之后插入 MatchAndCollect，命中规则按优先级排序后以【世界书】标签注入

### 细化需求3：前端世界书管理页面

- **front/src/views/world-book/WorldBookView.vue（新建）**：规则列表+在线测试面板（实时命中+高亮）+JSON导入导出
- **front/src/composables/useWorldBook.ts（新建）**
- **front/src/router/index.ts**：新增 /world-book 路由

### 改动影响范围汇总

| backend/internal/worldbook/（5文件） | 新建 | 完整模块 |
| backend/data/sql.sql | 修改 | 新表+索引 |
| backend/internal/chat/service.go | 修改 | 匹配注入 |
| backend/cmd/server/router.go | 修改 | 注册路由 |
| backend/cmd/server/main.go | 修改 | 初始化 |
| front/src/views/world-book/WorldBookView.vue | 新建 | 管理页 |
| front/src/composables/useWorldBook.ts | 新建 | API |
| front/src/router/index.ts | 修改 | /world-book |

---

## 七、上下文结构优化（系统1/用户/系统2/消息 结构）

### 当前现状

ProcessMessage() 中上下文拼接顺序为：system(禁止emoji + systemPrompt) → system(格式指令) → 遍历全部历史消息 → 用户当前消息。MITA9998 指出模型注意力是"两边高中间低"的 U 形曲线，当前结构把重要信息都放在了开头 system prompt 里，末尾只有用户消息，利用率不足。

### 细化需求1：messages 数组结构重组

- **chat/service.go**：ProcessMessage() 中 messages 数组顺序改为：
  `system1(系统prompt+长期画像+世界书命中) → 用户消息 → AI回复 → ... → system2(格式指令+短期工作记忆+本次检索注入)`
  核心逻辑：system1 放稳定的、大的上下文（角色设定、画像），system2 放动态的、小的提示（格式、工作记忆、检索结果），让注意力峰值覆盖两端。
- sys1Builder() 拼接：角色 systemPrompt + 画像摘要 + 世界书命中规则
- sys2Builder() 拼接：格式指令 + 工作记忆 + 向量检索结果 + 对话压缩摘要
- 不新增文件，纯重构现有 service.go 中的 messages 拼装逻辑

### 细化需求2：消息角色标记统一

- **chat/service.go**：历史消息遍历时给每轮消息打 `role: "user"` / `role: "assistant"` 标签（当前可能已有，需检查一致性）
- **agent/adapter/**：如有多处拼消息的逻辑，统一收敛到 service.go 中的 messagesBuilder

### 细化需求3：上下文结构可观测性

- **chat/model.go**：新增 ContextStructureLog 结构体，记录每轮 messages 数组实际长度、system1/system2 各自 token 量
- **chat/service.go**：debug 模式下将 ContextStructureLog 写入日志，方便调优注意力分布

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| backend/internal/chat/service.go | 修改 | messages 数组重组+sys1/sys2 builder |
| backend/internal/chat/model.go | 修改 | ContextStructureLog 结构体 |
| backend/internal/agent/adapter/（相关文件） | 检查 | 确认无散落拼装逻辑 |

**前端无需改动。**（结构变化对前端透明，但第十二章会在侧边栏展示注入统计供观测）

---

## 八、对话压缩优化（渐进式摘要树）

### 当前现状

完全没有对话压缩机制。loadHistory() 加载全部历史消息，对话越长上下文越大，token 消耗线性增长且超过模型窗口上限后只能截断（但截断逻辑也未实现）。

### 细化需求1：新建 compressor.go 压缩引擎

**新建 backend/internal/chat/compressor.go**

- Compressor 结构体：llmClient + summaryStore
- CompressTrigger：距上次压缩满16轮时触发，压缩最早4轮（即第1-4轮）
- CompressRound：调用 LLM 生成4轮对话的结构化摘要，含关键决策、用户偏好、待办事项
- 渐进式摘要树：第二次压缩时（第17-20轮），将第一次摘要+新的4轮合并再压缩，形成二层摘要
- 注入位置：压缩结果注入 system2 的"对话历史摘要"段

### 细化需求2：新建 conversation_summaries 表

- **data/sql.sql**：新增 conversation_summaries 表
  - id、conversation_id、round_start、round_end、summary_text、parent_summary_id、compressed_at
  - 索引：(conversation_id, round_start)、parent_summary_id

### 细化需求3：压缩触发集成

- **chat/service.go**：service 结构体加 compressor 字段
  - ProcessMessage 末尾调用 compressor.MaybeCompress(convID)
  - loadHistory() 跳过已压缩的早期轮次（include_in_context=0），改为注入压缩摘要
  - 压缩标记：被压缩的消息行 update include_in_context=0
- **cmd/server/main.go**：初始化 compressor 传入 chatSvc

### 细化需求4：前端压缩状态指示

- **front/src/views/web-chat/WebChatView.vue**：侧边栏底部新增"压缩状态"指示器（已压缩轮数/总轮数/最近压缩时间/摘要预览折叠面板）

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| backend/internal/chat/compressor.go | 新建 | 压缩引擎 |
| backend/data/sql.sql | 修改 | conversation_summaries 表+索引 |
| backend/internal/chat/service.go | 修改 | 集成 compressor+修改 loadHistory |
| backend/cmd/server/main.go | 修改 | 初始化 compressor |
| front/src/views/web-chat/WebChatView.vue | 修改 | 压缩状态指示器 |

---

## 九、6层记忆联动更新机制（统一管线）

### 当前现状

记忆更新散落在多个 goroutine 和独立调用中：ProcessMessage 中单独调 memory 更新、单独调 embedding 同步，各层之间无统一的更新触发入口和顺序保证。

### 细化需求1：新建 pipeline.go 管线调度器

**新建 backend/internal/memory/pipeline.go**

- 6层常量定义：
  `LayerWorkingMemory = 1`、`LayerProfile = 2`、`LayerEpisodic = 3`、`LayerStructuredFact = 4`、`LayerVector = 5`、`LayerGraph = 6`
- Pipeline 结构体：layers []PipelineLayer（按顺序执行）
- PipelineLayer 接口：`Process(ctx, convID, messages, newReply) error`
- 每层可以返回 Skip 信号（无新信息时跳过）
- 每层可以返回 RetryableError / FatalError
- pipeline.Execute() 顺序执行6层，记录每层耗时和状态

### 细化需求2：各层适配 PipelineLayer 接口

- **memory/working_memory.go**（第一章新建）：实现 PipelineLayer
- **profile/service.go**（第二章新建）：实现 PipelineLayer
- **episodic/service.go**（第三章新建）：实现 PipelineLayer
- **memory/service.go**（第四章改造）：现有 UpsertMemory 逻辑包装为 PipelineLayer
- **qdrant/client.go**（第五章改造）：向量同步逻辑包装为 PipelineLayer
- **graph/service.go**（第十章新建）：图同步逻辑包装为 PipelineLayer

### 细化需求3：替换散落的 goroutine

- **chat/service.go**：ProcessMessage 末尾用 `go pipeline.Execute(...)` 替代现有的多个 `go xxx()` 调用
- pipeline 内部串行执行6层，确保画像更新完再同步图关系

### 细化需求4：前端管线状态监控

- **front/src/views/memory-manager/MemoryManagerView.vue**：顶部新增"管线状态"横条（6层图标+状态灯，绿色=完成/灰色=跳过/红色=失败，hover 显示耗时）
- **backend/cmd/server/router.go**：新增 GET /api/memory/pipeline/status 返回最近一次管线执行详情

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| backend/internal/memory/pipeline.go | 新建 | 管线调度器 |
| backend/internal/chat/service.go | 修改 | 替换散落 goroutine |
| backend/internal/memory/service.go | 修改 | 适配 PipelineLayer |
| backend/internal/memory/working_memory.go | 修改 | 适配 PipelineLayer |
| backend/internal/profile/service.go | 修改 | 适配 PipelineLayer |
| backend/internal/episodic/service.go | 修改 | 适配 PipelineLayer |
| backend/internal/qdrant/client.go | 修改 | 适配 PipelineLayer |
| backend/internal/graph/service.go | 修改 | 适配 PipelineLayer |
| backend/cmd/server/router.go | 修改 | /api/memory/pipeline/status |
| front/src/views/memory-manager/MemoryManagerView.vue | 修改 | 管线状态横条 |

---


## 十、轻量图存储（SurrealDB 嵌入式替代 Neo4j）

### 当前现状

项目无图数据库依赖。千夜提到 Neo4j 在4G内存服务器上消耗700MB，不适合轻量部署。经调研后选用 **SurrealDB**——唯一同时满足 Go SDK、嵌入式部署、支持图关系查询、活跃维护四项条件的图数据库。嵌入运行时空载 ~30MB，万边级 ~80MB，完全适配4G服务器。

### 细化需求1：引入 SurrealDB 依赖与嵌入式启动

- **backend/go.mod**：新增 `github.com/surrealdb/surrealdb.go` 依赖
- **backend/cmd/server/main.go**：
  - 启动时拉起 SurrealDB 嵌入式进程（`surrealdb start --log trace --user root --pass root memory` 或文件持久化到 `data/graph.db`）
  - 注册优雅关闭：`defer surrealdb.Stop()`
  - 初始化 graphSvc，传入 chatSvc 和 pipeline
- **backend/internal/config/config.go**：新增 SurrealConfig（Endpoint、Namespace、Database、Username、Password）
- **backend/internal/config/config.yml**：新增 surrealdb 配置段

### 细化需求2：新建 graph/ 模块（SurrealDB 适配层）

**新建 backend/internal/graph/ 模块（client/service/handler/router 4文件）**

- **graph/client.go**：SurrealDB 连接管理
  - NewClient(cfg) 创建连接，指定 namespace="uai"、database="memory_graph"
  - 定义节点表结构：`DEFINE TABLE entity_node SCHEMAFULL` + `DEFINE FIELD entity_type TYPE string` + `DEFINE FIELD label TYPE string` + `DEFINE FIELD properties TYPE flexible`
  - 定义关系边表：`DEFINE TABLE entity_edge TYPE RELATION IN entity_node TO entity_node SCHEMAFULL` + `DEFINE FIELD relation_type TYPE string` + `DEFINE FIELD weight TYPE float`
  - 定义索引加速图遍历
- **graph/service.go**：图操作封装
  - SyncNode(entityType, entityID, label, properties)：`CREATE entity_node:${id} CONTENT {...}` 或 `UPDATE`
  - SyncEdge(sourceID, targetID, relationType, weight)：`RELATE entity_node:${src}->entity_edge->entity_node:${dst} SET relation_type=..., weight=...`
  - QueryNeighbors(entityID, depth=2)：原生图遍历 `SELECT ->entity_edge->entity_node AS neighbors FROM entity_node:${id}` 递归 depth 次
  - FindPaths(sourceID, targetID, maxDepth=4)：SurrealDB 图遍历
  - DeleteOrphanNodes()：清理无边节点
- **graph/handler.go**：HTTP API
  - GET /api/graph/node/:id/neighbors?depth=2
  - GET /api/graph/path?from=...&to=...&maxDepth=4
  - GET /api/graph/stats（节点数/边数/按类型分布）
  - DELETE /api/graph/node/:id（级联删边）

### 细化需求3：图关系自动同步（Hook 机制）

- 画像写入（profile/service.go UpsertConfidence）后 hook：
  ```
  graphSvc.SyncNode("profile", attrName, attrValue, props)
  graphSvc.SyncEdge("user:default", "profile:"+attrName, "has_profile", confidence/100)
  ```
- 情景写入（episodic/service.go SaveEpisode）后 hook：
  ```
  graphSvc.SyncNode("episodic", episodeID, title, props)
  graphSvc.SyncEdge("user:default", "episodic:"+episodeID, "experienced", 1.0)
  // 与相关画像节点关联
  if episode.RelatedProfiles { graphSvc.SyncEdge("episodic:"+episodeID, "profile:"+pid, "related_to", 0.8) }
  ```
- 世界书规则命中时（worldbook/service.go MatchAndCollect）：
  ```
  graphSvc.SyncEdge("worldbook:"+ruleID, "memory:"+memID, "triggered_by", matchScore)
  ```

### 细化需求4：前端图可视化

- **front/src/views/graph/GraphView.vue（新建）**：
  - ECharts 力导向图渲染 SurrealDB 返回的节点+边数据
  - 节点类型筛选（user/profile/episodic/memory/worldbook），不同类型用不同颜色
  - 关系类型筛选复选框
  - 深度控制滑块（2跳/3跳/4跳）
  - 节点点击展开详情面板（标签+类型+属性JSON+关联列表）
  - 搜索框：输入节点ID关键词，高亮匹配路径
  - 全屏模式
- **front/src/composables/useGraph.ts（新建）**：封装 /api/graph/* 调用
- **front/src/router/index.ts**：新增 /graph 路由
- **front/src/views/memory-manager/MemoryManagerView.vue**：图谱标签页嵌入迷你力导向图（3跳以内概览）

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| backend/go.mod | 修改 | 新增 surrealdb.go 依赖 |
| backend/internal/graph/client.go | 新建 | SurrealDB 连接+表定义 |
| backend/internal/graph/service.go | 新建 | 图操作封装 |
| backend/internal/graph/handler.go | 新建 | HTTP API |
| backend/internal/graph/router.go | 新建 | 路由注册 |
| backend/internal/config/config.go | 修改 | SurrealConfig |
| backend/internal/config/config.yml | 修改 | surrealdb 配置段 |
| backend/internal/profile/service.go | 修改 | hook SyncNode+SyncEdge |
| backend/internal/episodic/service.go | 修改 | hook SyncNode+SyncEdge |
| backend/internal/worldbook/service.go | 修改 | hook triggered_by |
| backend/cmd/server/main.go | 修改 | 启动/关闭 SurrealDB |
| backend/cmd/server/router.go | 修改 | 注册 /api/graph/* |
| front/src/views/graph/GraphView.vue | 新建 | ECharts 力导向图 |
| front/src/composables/useGraph.ts | 新建 | API |
| front/src/router/index.ts | 修改 | /graph |
| front/src/views/memory-manager/MemoryManagerView.vue | 修改 | 图谱标签页 |

### 与 SQLite 方案的差异

| | SQLite两张表（旧方案） | SurrealDB（新方案） |
|--|--|--|
| 图遍历 | 手写递归CTE | `SELECT ->edge->node` 原生语法 |
| 关系存储 | entity_edges 自建表 | 原生 RELATE 语句 |
| 级联删除 | 手动删边 | 自动级联 |
| go.mod 新增依赖 | 0 | +1 (surrealdb.go) |
| 启动方式 | 文件打开即可 | 子进程或嵌入式 |
| 内存占用 | ~0（复用 SQLite） | ~30-80MB |
| Redis 缓存 | 需要（cache.go） | 不需要（SurrealDB 自带索引） |

## 十一、记忆精准检索优化（时间衰减+频率加权+语义去重）

### 当前现状

ProcessMessage 中调用 Qdrant 语义搜索检索5条相关记忆，但搜索逻辑简单：直接用用户消息 embedding 查 top-5，无时间衰减、无频率加权、无去重。

### 细化需求1：新建 retrieval.go 检索优化器

**新建 backend/internal/memory/retrieval.go**

- RetrievalScorer 结构体：
  - TimeDecay(createdAt, halflifeDays)：按记忆 type 设置不同半衰期（情景30天、画像90天、结构化事实180天、世界书365天），越新权重越高
  - FrequencyBoost(accessCount)：log(1+accessCount) 对数加权，高频访问的记忆提权
  - JaccardDedup(results, threshold=0.85)：两两计算语义相似度，相似度>阈值的内容只保留时间更新的那条
- RetrievalPipeline：
  - Qdrant 初检 top-20（而非 top-5）
  - 时间衰减加权重排 → 频率加权 → Jaccard去重 → 截断取 top-5
  - 结果注入 system2 的上下文

### 细化需求2：检索日志与反馈

- **data/sql.sql**：新增 retrieval_logs 表
  - id、conversation_id、query_text、retrieved_memory_ids(JSON)、scoring_details(JSON)、created_at
  - 索引：(conversation_id, created_at)
- memory/service.go：每次检索记录日志，供后续分析检索质量
- GET /api/memory/retrieval/stats 返回检索命中率趋势

### 细化需求3：前端检索质量面板

- **front/src/views/memory-manager/MemoryManagerView.vue**：标签页新增"检索分析"面板（检索命中率趋势折线图、各半衰期参数滑块预览、最近检索日志表格）

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| backend/internal/memory/retrieval.go | 新建 | 检索优化器 |
| backend/data/sql.sql | 修改 | retrieval_logs 表 |
| backend/internal/memory/service.go | 修改 | 集成 RetrievalPipeline |
| backend/internal/chat/service.go | 修改 | 使用新检索结果注入 |
| backend/cmd/server/router.go | 修改 | /api/memory/retrieval/stats |
| front/src/views/memory-manager/MemoryManagerView.vue | 修改 | 检索分析面板 |

---

## 十二、前端记忆管理聚合（统一入口+新增页面）

### 当前现状

前端已有 MemoryManagerView（记忆 key-value 管理）和 MemoryTimeline（记忆事件时间线）。但缺少对新模块（画像、情景、世界书、图谱）的统一入口，且 WebChatView 缺少记忆注入的透明展示。

### 细化需求1：MemoryManagerView 全面升级

- **front/src/views/memory-manager/MemoryManagerView.vue**：
  - 顶部标签页从 [全部记忆] 扩展为 [全部记忆 | 用户画像 | 情景记忆 | 结构化事实 | 世界书 | 图谱]
  - 每个标签页嵌入对应子组件或内联渲染（画像卡片、情景时间线、事实表格、规则列表、迷你力图谱）
  - 全局搜索栏跨所有类型搜索
  - 批量操作：多选删除/重新提取/导出JSON
  - 共识机制：同一事实多轮出现自动标记"已共识"，置信度≥80 打绿色标签

### 细化需求2：MemoryTimeline 增强

- **front/src/views/memory-timeline/MemoryTimeline.vue**：
  - 左侧新增"层级筛选"复选框组（工作记忆/画像/情景/事实/世界书/图谱），可多选叠加显示
  - 时间线节点按层级着色（6种颜色对应6层）
  - Diff 模式：选择两个时间点进行记忆差异对比（新增/修改/删除）
  - CSV 导出按钮（含层级、类型、时间、内容四列）

### 细化需求3：WebChatView 记忆透明显面板

- **front/src/views/web-chat/WebChatView.vue**：
  - 右侧栏新增"记忆注入"折叠面板：
    - 当前轮检索到的记忆列表（来源层级+内容摘要+相似度分数）
    - 当前用户画像摘要（卡片式，如"用户怕狗 ✅ 置信度92%"）
    - 当前世界书命中规则（规则名+匹配文本高亮）
    - 压缩状态指示器（已压缩轮数/总轮数/最近压缩时间）
    - 管线执行状态灯（6层完成/跳过/失败，hover显示耗时）
  - 面板底部"反馈"按钮：用户可标记某条注入记忆"不相关"或"错误"，作为检索反馈信号

### 细化需求4：导航菜单整合

- **front/src/...**：主导航或侧边栏新增记忆管理菜单组：
  - 记忆总览（/memory-manager）
  - 用户画像（/profiles）
  - 情景记忆（/episodic）
  - 世界书（/world-book）
  - 记忆图谱（/graph）
  - 记忆时间线（/memory-timeline）
- 新增顶部下拉或 Tab 导航切换到不同记忆子页面

### 细化需求5：前端 API 层统一

- **front/src/composables/useMemoryHub.ts（新建）**：统一记忆 API 聚合层，封装
  - useMemory（现有）
  - useProfile（第二章新建）
  - useEpisodic（第三章新建）
  - useWorldBook（第六章新建）
  - useGraph（第十章新建）
  - useMemoryPipeline（第九章新建）
  - useRetrieval（第十一章新建）
- MemoryManagerView 使用 useMemoryHub 统一获取各层数据

### 改动影响范围汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| front/src/views/memory-manager/MemoryManagerView.vue | 修改 | 标签页扩展+全局搜索+批量操作 |
| front/src/views/memory-timeline/MemoryTimeline.vue | 修改 | 层级筛选+着色+diff+CSV导出 |
| front/src/views/web-chat/WebChatView.vue | 修改 | 记忆注入面板+反馈按钮 |
| front/src/composables/useMemoryHub.ts | 新建 | 统一API聚合层 |
| front/src/router/index.ts | 修改 | 新增 /graph、/profiles、/episodic、/world-book |
| front/src/...（导航组件） | 修改 | 记忆管理菜单组 |


---

## 附录：为什么选 SurrealDB 替代 Neo4j（完整决策记录）

### 问题

千夜提到 Neo4j 在4G内存服务器上消耗700MB，不适合轻量部署。需要找到替代方案。

### 候选方案对比

| | Neo4j | Cayley | SQLite两张表 | SurrealDB |
|--|-------|--------|-------------|-----------|
| 语言 | Java | Go | — | Rust |
| 部署 | 独立服务 | 嵌入式 | 嵌入式 | **嵌入式** |
| 图查询 | Cypher | Gremlin | 手写SQL | SurrealQL 原生 RELATE |
| Go SDK | 有 | 有 | database/sql | **官方 surrealdb.go** |
| 最新版本 | 5.x | v0.7.7(2019) | — | **v3.1.4(2026-06)** |
| 内存占用 | 700MB+ | ~30MB | ~0 | ~30-80MB |
| 启动速度 | 10-30s | 即时 | 即时 | **<1s** |

### 选择 SurrealDB 的理由

1. **唯一满足四条件**：Go SDK + 嵌入式 + 图关系 + 活跃维护
2. **Cayley 已死**（2019年停更），Dgraph 是独立服务非嵌入式
3. SurrealQL 的 `RELATE` 和 `SELECT ->edge->node` 语法比手写递归 SQL 简洁得多
4. 嵌入模式下空载30MB，万边级80MB——4G服务器完全够用
5. Qdrant 继续负责向量检索，SurrealDB 只负责图关系，各司其职

---

*文档生成时间：2026-06-17*
RETIRED LEGACY BASELINE

This dated baseline is historical only and MUST NOT be used by release, CI, source packing, or finalization gates.
The single canonical baseline is:

DESKTOP_PET_FINALIZATION_SHA256SUMS.txt

Regenerate and verify it only through:

node scripts/build-freeze-scope.mjs --write
node scripts/build-freeze-scope.mjs --verify

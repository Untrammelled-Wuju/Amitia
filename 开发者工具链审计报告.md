# Amitia 开发者工具链审计报告

## 审计时间：2026-07-27

## 一、后端 Extension Kernel 子系统状态

### 核心装配问题（container_builder.go）
约 60% 核心组件处于非功能性占位状态：
- ExecutionPipeline：所有字段 nil，执行返回 "no dispatcher configured"
- LifecycleManager：NewManager(nil,nil,nil,nil)，无法执行生命周期计划
- DependencyResolver：CandidateProvider 为 nil，所有依赖解析返回 StatusMissing
- PermissionBroker：内存存储，重启后授权丢失
- ScopeManager：内存存储，重启后作用域丢失

### 逐子系统状态

| 子系统 | 实现状态 | 持久化 | 接线 | 关键缺口 |
|--------|---------|--------|------|---------|
| dev_mode | 部分 | 内存 | 否 | 无Drain、无双注册防护、无实际编译、Validate/Load/Activate空操作 |
| developer_console | 部分 | 内存 | 否 | aggregators未注入，BuildOverview返回空 |
| amitiax | 部分 | 否 | 否 | 无签名验证（库层面有ed25519但未调用） |
| extension_center | 部分 | 内存 | 否 | CardProvider未注入 |
| extension_detail | 部分 | 内存 | 否 | DetailProvider未注入 |
| manifest_v2 | 完整 | 否 | N/A | 无JSON Schema、无行列定位、kind白名单不完整 |
| contribution | 完整 | 内存 | 否 | 无DB持久化 |
| capability | 完整 | 内存 | 否 | 无DB持久化 |
| execution | 部分 | 内存 | 否 | pipeline未接线；PermissionGate nil时跳过(默认允许) |
| lifecycle | 完整 | 条件 | 否 | 独立运行 |
| lifecycle_manager | 占位 | 否 | 否 | 全nil参数 |
| host_api | 完整 | 内存 | 否 | 安全检查Setter未调用 |
| dependency | 完整/占位 | 否 | 否 | CandidateProvider nil |
| enablement | 完整 | SQLite | 是 | - |
| hook | 完整 | SQLite | 是 | - |
| event | 完整 | SQLite | 是 | - |
| schedule | 完整 | SQLite | 是 | 安全检查器和执行器未注入 |
| domain | 部分 | SQLite | 是 | - |
| javascript_main | 完整 | 否 | 是 | - |
| runtime_supervisor | 完整 | 内存 | 是 | - |

### 安全隐患
- S1: ExecutionPipeline 默认允许（PermissionGate nil时跳过）
- S2: 所有安全门控在container中未接线
- S3: amitiax无签名验证
- S4: 热重载无Drain逻辑
- S5: 热重载双注册风险
- S6: host_api安全检查未注入
- S7: dev_mode无实际TypeScript编译
- S8: desktop_extension UnregisterByExtension不完整

## 二、前端状态

### 缺失项（完全不存在）
- 开发模式开关UI
- Workspace管理UI
- 热重载状态展示
- Manifest实时校验展示（含文件/路径/行列）
- Contribution诊断视图
- Permission调试视图
- Scope调试视图
- Operation/Invocation Trace视图
- 日志流视图
- 错误中心
- ExtensionDetailView（统一详情页）

### 部分存在
- Runtime诊断：RuntimeDebugView孤立死代码（未接入路由），PluginDetailView有Circuit重置
- Hook/Event/Schedule：/kernel/hooks /kernel/events /kernel/schedules 系统管理页（非开发者诊断）
- 诊断包导出：仅通用维护诊断包
- Pack/Sign/Verify：API层有signer函数但前端无UI

### 旧入口
- 旧扩展系统已冻结（FROZEN.md, 2026-07-25）
- 入口：/extensions（Plugin/Skill/MCP/AgentSkill/Workshop）
- 新入口：/kernel（KernelCenterView + 子系统页）

## 三、CLI/打包/签名/测试状态

### CLI：完全缺失
- 无 tools/amitiax-cli
- 无任何CLI命令（init/validate/dev/inspect/test/pack/sign/verify/doctor/export-diagnostics）

### 打包签名：库层面完整
- amitiax/package.go：真实zip解析、路径穿越防护、SHA-256树哈希
- package_security/：ed25519验签、原子提交、staging、私钥检测
- trust/signature.go：ed25519签名生成
- 缺口：签名未在amitiax中调用，未通过CLI暴露

### 诊断导出：缺失
- 无export-diagnostics
- 仅有observability/export.go审计导出（有脱敏）

### 日志系统：部分
- 有脱敏（apiKey/bearer/sk/手机号/身份证）
- 有轮转（7天/24小时）
- 有trace（request_id/correlation_id等）
- 缺限流、缺按Extension过滤

### 测试：部分
- 单元测试真实
- final_acceptance/suite.go：27项中仅1项真实验证，其余占位返回成功
- equivalence/suite.go：30项中仅6项真实验证，其余硬编码结果
- 无固定Passed=true问题（构造数据除外）

### 签名密钥：完整
- 无私钥明文存储
- 测试密钥临时生成
- 入库检测拦截私钥

## 四、实施优先级

1. 后端container装配修复（基础）
2. dev_mode完善（持久化/Drain/双注册/Generation）
3. manifest_v2修复（行列/Schema/完整kind）
4. amitiax签名接线
5. 诊断API实现
6. 诊断导出实现
7. CLI实现（10命令）
8. 前端开发者控制台
9. 测试扩展+单元+集成+E2E+故障注入+安全
10. 旧入口迁移+最终验收

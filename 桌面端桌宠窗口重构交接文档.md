# Amitia 桌宠系统 第5步 Electron 桌宠窗口重构 - 交接文档

## 实施概述

本文档记录了 Amitia 桌宠系统第5步 Electron 桌宠窗口重构的完整实施情况，涵盖后端 Runtime Bridge 协议接线、RuntimeSettings 增强、Electron 窗口管理重构、Runtime Bridge 客户端集成等全部内容。

## 一、后端变更

### 1.1 数据库迁移

**新增迁移文件:**
- `backend/internal/migration/desktop_pet_runtime_clients.go` - 运行时客户端表
- `backend/internal/migration/desktop_pet_runtime_commands.go` - 运行时命令表
- `backend/internal/migration/desktop_pet_runtime_actual_states.go` - 实际状态表
- `backend/internal/migration/desktop_pet_runtime_settings_v2.go` - RuntimeSettings V2 增强字段

**baseline.sql 更新:**
- 新增 `desktop_pet_runtime_clients`、`desktop_pet_runtime_commands`、`desktop_pet_runtime_actual_states` 三张运行时表及索引
- `desktop_pet_runtime_settings` 表新增 `revision`、`updated_by`、`recenter_screen_id` 字段

**migrations.go 更新:**
- 注册 `DesktopPetRuntimeClientsMigration`、`DesktopPetRuntimeCommandsMigration`、`DesktopPetRuntimeActualStatesMigration`
- 注册 `DesktopPetRuntimeSettingsV2Migration`
- 所有迁移按版本号有序执行，带 Checksum 校验

### 1.2 RuntimeSettings 增强 (Phase B)

**model.go:**
- RuntimeSettings 结构体新增 `Revision`、`UpdatedBy`、`RecenterScreenID` 字段

**dto.go (新增):**
- 强类型 `UpdateSettingsDTO`，包含字段级验证
- `RecenterDTO` 用于重置位置命令

**service.go:**
- 用户所有权验证 (userId 校验)
- 基于修订号的乐观并发控制 (CAS)
- Recenter 命令语义正确实现

**repository.go:**
- 新增 `UpdateSettingsWithCAS` 方法，基于 revision 进行条件更新

**handler.go:**
- 使用 DTO 替代 map[string]interface{}
- 传递 userId 进行权限校验

**errors.go:**
- 新增 `ErrRevisionConflict` 修订号冲突错误

### 1.3 Runtime Bridge 协议接线 (Phase C)

**handler.go:**
- `HandleEvent` - 事件持久化到 actual_state 表
- `HandleHeartbeat` - 心跳持久化到 actual_state 表
- `HandleResult` - 命令结果持久化 actual_state
- 连接关闭时标记客户端离线

**connection.go:**
- `onHeartbeat` 回调签名更新，传递 runtimeID 和 sessionID

**service.go:**
- 传递 StateStore 给 Handler，支持状态持久化

### 1.4 Runtime Bridge 路由

**已注册路由:**
- `GET /internal/desktop-pet/runtime/ws` - WebSocket 升级端点
- `GET /api/desktop-pets/runtime/status` - 运行时状态列表
- `GET /api/desktop-pets/runtime/status/:runtimeId` - 单个运行时状态
- `GET /api/desktop-pets/runtime/metrics` - 运行时指标
- `GET /api/desktop-pets/runtime/bootstrap-token` - 引导令牌获取

## 二、Electron 桌宠窗口重构

### 2.1 阶段1: 基础设施

**runtime-bridge-client.ts (新增):**
- 完整的 WebSocket 客户端实现
- 协议版本: `amitia-desktop-pet.v1`
- 消息类型: control / command / result / event
- 自动重连（指数退避）
- 心跳定时器
- 命令分发: spawn / destroy / show / hide / play_action / update_settings / recenter / sync
- 状态探查: state_probe
- 关闭处理: shutdown / superseded

**pet-preload.ts (新增):**
- 专用 preload 脚本，最小化 IPC 暴露
- 接收回调: onFrameUpdate / onActionSwitch / onLoadError / onState / onChatState
- 发送方法: sendClick / sendDoubleClick / sendHover
- contextBridge + contextIsolation 安全隔离

**vite.config.ts 更新:**
- 多入口 preload 构建 (index + pet-preload)
- `copyPetHtmlPlugin` 插件，开发时自动复制 pet.html 到 dist/renderer/

**window-adapter.ts 更新:**
- preload 路径: `../preload/pet-preload.cjs` (专用)
- `sandbox: true` (沙箱模式启用)
- 创建后加载 `pet.html` 渲染页面

### 2.2 阶段2: 窗口管理

**Geometry/Display/DPI:**
- 多显示器支持 (listScreens, findScreenById)
- DPI 感知 (getDpiScale, scaleFactor)
- 位置持久化使用 DIP 坐标 (snapshotRuntimePosition)
- 屏幕变更自动调整 (ensureVisible, handleDisplayRemoved)
- display-metrics-changed 事件监听

**Click-through 三模式:**
- `none` - 完全可点击
- `alpha` - 像素级 Alpha 通道命中测试 (ClickThroughController + ClickHitTester)
- `boundingBox` - 包围盒模式
- 拖动时自动禁用穿透

**手势识别:**
- 单击 (clicked) - 带冷却去抖
- 双击 (double_clicked) - 阈值检测
- 悬停 (hovered) - 冷却控制
- 拖动 (dragged/dropped) - DragController
- 事件通过 IPC 从渲染进程传递到主进程

**pet.html (新增):**
- 透明背景 Canvas 渲染
- DPI 感知 (devicePixelRatio)
- 帧图片 dataURL 渲染
- 鼠标事件采集 (click/dblclick/mousemove)
- 错误覆盖层显示

### 2.3 阶段3: Runtime Bridge 集成

**manager.ts 集成:**

- `RuntimeBridgeClient` 实例化管理
- `startBridge()` / `stopBridge()` 生命周期
- `connectBridge()` - 获取 bootstrap token 并建立 WebSocket 连接
- `scheduleBridgeReconnect()` - 断线重连（2秒延迟）
- `buildBridgeCallbacks()` - 命令回调映射:
  - `onSpawn` → `enableInstallation(installationId)`
  - `onDestroy` → `disableInstallation()`
  - `onShow` → `window.show()`
  - `onHide` → `window.hide()`
  - `onPlayAction` → `playAction(actionKey)`
  - `onUpdateSettings` → `updateSettings(updates)`
  - `onRecenter` → `recenter()`
  - `onSync` → 状态对账 (ensureAbsent / desiredPet)
  - `onStateProbe` → `collectPetInstanceSummary()`
  - `onShutdown` → `shutdown()`
  - `onConnected` → `syncBridgeState()`
  - `onDisconnected` → `scheduleBridgeReconnect()`

- `collectPetInstanceSummary()` - 构建 PetInstanceSummary:
  - petInstanceId, installationId, visible, currentActionKey
  - positionX, positionY, screenId, scale

- `sendBridgeEvent()` - 事件上报:
  - action_switch (动作切换)
  - drag_start / drag_end (拖动事件)

- `syncBridgeState()` - 状态同步:
  - 在 setState() 中自动调用
  - 更新 bridgeClient 的 petInstances 列表

- `currentActionKey` 跟踪:
  - handleActionSwitch() 中更新
  - stopRuntime() 中清除

## 三、验证结果

### 3.1 编译验证
- TypeScript typecheck: 零错误
- Go 后端编译 (CGO_ENABLED=1): 成功
- Vite 构建: pet-preload.cjs (1.52 kB) + index.cjs (6.40 kB) + main/index.js (816.98 kB)

### 3.2 测试验证
- 前端宠物模块测试: 116 passed (4 test files)
- 后端 runtime 模块: 编译通过 (无测试文件)
- 后端 installation 模块: 2 个预存 Windows 文件系统时序失败 (非本次变更)
- 后端 migration 模块: CGO 环境限制 (非本次变更)

### 3.3 服务状态
- 后端 (18899): health=true, ready=true, db=ok
- 前端 (5178): status=200
- SurrealDB (18000): status=200
- Qdrant (19178): status=200
- 微信侧车: connected
- QQ 侧车: running

## 四、文件变更清单

### 新增文件
| 文件路径 | 说明 |
|---------|------|
| `desktop/src/main/pet/runtime-bridge-client.ts` | WebSocket 客户端 |
| `desktop/src/preload/pet-preload.ts` | 宠物窗口专用 preload |
| `desktop/src/renderer/pet.html` | 宠物窗口渲染页面 |
| `backend/internal/desktoppet/installation/dto.go` | 强类型 DTO |
| `backend/internal/desktoppet/installation/errors.go` | 修订号冲突错误 |
| `backend/internal/migration/desktop_pet_runtime_settings_v2.go` | Settings V2 迁移 |
| `desktop-pet-window-integration-map.md` | 集成映射文档 |

### 修改文件
| 文件路径 | 说明 |
|---------|------|
| `desktop/src/main/pet/manager.ts` | Runtime Bridge 集成 |
| `desktop/src/main/pet/window-adapter.ts` | 专用 preload + sandbox + loadFile |
| `desktop/src/preload/pet-preload.ts` | 新增交互发送方法 |
| `desktop/vite.config.ts` | 多入口 preload + copyPetHtmlPlugin |
| `desktop/package.json` | 构建脚本增加 pet.html 复制 |
| `backend/internal/migration/migrations.go` | 注册新迁移 |
| `backend/internal/migration/baseline.sql` | 新增表和字段定义 |
| `backend/internal/desktoppet/installation/model.go` | RuntimeSettings 新字段 |
| `backend/internal/desktoppet/installation/service.go` | CAS + 用户所有权 |
| `backend/internal/desktoppet/installation/repository.go` | CAS 更新方法 |
| `backend/internal/desktoppet/installation/handler.go` | DTO + userId |
| `backend/internal/desktoppet/runtime/handler.go` | 事件/心跳/结果持久化 |
| `backend/internal/desktoppet/runtime/connection.go` | 心跳回调签名 |
| `backend/internal/desktoppet/runtime/service.go` | 传递 StateStore |

## 五、已知限制与后续工作

1. **pet.html 渲染页面**: 当前为基础 Canvas 渲染，后续可增强为 Vue 组件以支持更复杂的 UI
2. **Runtime Bridge 认证**: 当前使用 bootstrap token，生产环境可能需要更安全的认证机制
3. **多宠物实例**: 当前架构支持单实例，多实例需要扩展 manager 的实例管理逻辑
4. **Windows 文件系统时序**: installation 测试中的卸载测试在 Windows 上存在时序问题（预存）
5. **CGO 测试环境**: migration 测试需要 CGO_ENABLED=1，CI 环境需配置

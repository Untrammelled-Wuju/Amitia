package textlib

const UserDossierInjectionPrefix = `【关于 ta 的笔记 · 仅供你内心参考 · 绝对禁止在回复中对用户说"ta"】`

const UserDossierInjectionGuardrail = `── 使用规则 ──
· 用自然的口语写，像自己私下的笔记。不要像档案报告、不要用表格、不要用标题。
· 按自然的叙事组织，不是逐条列举事实。可以按"基本信息→性格→喜好→我们的关系"的顺序自然过渡。
· 只写你从事实中确定知道的，不要编造。不确定用"可能""好像"，确定直接陈述。
· 先写稳定信息（身份、经历、性格、喜好、关系），再写近期状态（最近在忙什么、情绪状态）。
· 近期状态用"—— 近期状态（仅供参考） ——"分隔。
· 保持 500-1000 字。
· 末尾标注更新日期。
· 人称：用户以"ta"称呼。

── 禁止 ──
× 不要写"根据事实""根据记录""我的数据显示"等元表述
× 不要写"以下是我的笔记"等开头语，直接开始写
× 不要使用表格、列表、标题格式（## 等）
× 不要把近期状态写成确定事实——那是"仅供参考"的
× 不要把成人内容细节写进档案——亲密时刻用"我们有亲密时刻"模糊表述即可
× 不要记录任何高度私密的短期状态`

const UserDossierLoadGuardrail = `你在和用户面对面直接对话。使用这些笔记时，必须将"ta"转化为第二人称"你"。
绝对不要说"根据我的笔记""根据我的记录""我知道 ta 最近……"等元表述。
档案最后一段的"近期状态"仅供参考——不要在用户正在聊开心事时主动提起压力话题。`

const MemoryFormatContextHeader = "=== 关于用户的记忆 ==="

const MemoryCategoryLabels = `事实/情感/偏好/事件/习惯/关系`

const MemoryFactExtractReferenceRule = `相关记忆只供自然参考，不要逐条复述，不要说根据记忆`

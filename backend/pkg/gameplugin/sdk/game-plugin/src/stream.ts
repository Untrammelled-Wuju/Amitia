/**
 * Stream RPC helpers are intentionally not part of GameHost protocol v1.
 * Use declared channels for plugin-to-host streams and plugin RPC for
 * host-to-plugin traffic until a bidirectional stream protocol is negotiated.
 */
export {};

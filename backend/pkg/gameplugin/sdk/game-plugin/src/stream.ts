/**
 * Stream RPC helpers are intentionally not part of GameHost protocol v1.
 * Use declared channels for plugin-to-host, host-to-plugin, or bidirectional
 * message streams. Dedicated stream RPC lifecycle methods remain out of v1.
 */
export {};

server:
  port: 8899
  host: "127.0.0.1"
  mode: "debug"

storage:
  dataDir: "data"

jwt:
  secret: "u-ai-secret-key-change-me"
  expireDays: 7

app:
  name: "U-Ai"
  version: "1.0.0"
  deployMode: "desktop-local"
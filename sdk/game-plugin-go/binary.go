package sdk

// Binary streaming is intentionally not part of amitia-game-host/1.
//
// The internal binary registry remains available to the host for lifecycle and
// future protocol work, but no public binary.register/binary.release RPC is
// exposed until a production provider, transport and admission path are wired
// end to end.
